export default function Footer() {
  return (
    <div
      style={{
        background: "#f3f4f6",
        borderTop: "1px solid #ccc",
        padding: "15px",
        textAlign: "center",
        marginTop: "40px",
        fontSize: "14px",
        color: "#555"
      }}
    >
      Made by <strong style={{ cursor: "pointer" }}>
        Arin, Reshma, Anshika, Vedika
        </strong>
    </div>
  );
}